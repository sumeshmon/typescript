//  ++++++ if the god is passed under items if we mouse hover TS will display the types automatically.
var gods = ['Krishna', 'Shiva'];
// const gods = [10,20,30]
var allGods = gods.map(function (items) {
    return items;
    return 'sumesh';
});
console.log(allGods);
